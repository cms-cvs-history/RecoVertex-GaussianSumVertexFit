process Demo =  {
  #keep the logging output to a nice level
  include "FWCore/MessageLogger/data/MessageLogger.cfi"

untracked PSet maxEvents = {untracked int32 input = 1}

source = PoolSource { 
  untracked vstring fileNames = {"file:../../KalmanVertexFit/test/sim_10muons_1-10GeV.root"} 
}

include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
include "TrackingTools/TransientTrack/data/TransientTrackBuilder.cfi"

module dump = EventContentAnalyzer {}

module trackMixTest = TrackMix {
     string gsfTrackLabel = "GsfElectrons"
     string ckfTrackLabel = "ctfWithMaterialTracks"
}

path p = {dump,trackMixTest } 
} 
